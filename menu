<!-- templates/menu.html -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Menu - Plate & Chill</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
    <style>
        body { font-family: Arial, sans-serif; background: #f9f9f9; color: #333; }
        .container { width: 80%; margin: 30px auto; }
        h1 { text-align: center; margin-bottom: 20px; }
        table { width: 100%; border-collapse: collapse; }
        th, td { padding: 12px; text-align: left; border-bottom: 1px solid #ddd; }
        th { background-color: #f2f2f2; }
        tr:hover { background-color: #f5f5f5; }
        .category { font-weight: bold; margin-top: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Menu</h1>

        <h2 class="category">Foods</h2>
        <table>
            <tr>
                <th>Name</th>
                <th>Price (₱)</th>
            </tr>
            {% for item in menu_items if item.category == 'Food' %}
            <tr>
                <td>{{ item.name }}</td>
                <td>{{ item.price }}</td>
            </tr>
            {% endfor %}
        </table>

        <h2 class="category">Drinks</h2>
        <table>
            <tr>
                <th>Name</th>
                <th>Price (₱)</th>
            </tr>
            {% for item in menu_items if item.category == 'Drink' %}
            <tr>
                <td>{{ item.name }}</td>
                <td>{{ item.price }}</td>
            </tr>
            {% endfor %}
        </table>
    </div>
</body>
</html>

